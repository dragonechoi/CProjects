#include <stdio.h>
//성적 입력받기 -> 학점 계산
void main()
{  
	//printf("\n점수를 입력하면 계산해 주는 프로그램 입니다.\n \n");

	//int score;//점수 저장용

	//printf("점수 입력 : ");
	//scanf_s("%d", &score);
	//
	////점수의 따른 학점을 계산 학점은 한문자 이니까.
	//char grade;
	//grade = (score >= 90 && score<=100) ? 'A' :
	//	    (score >= 80 && score<90) ? 'B' :
	//	    (score >= 70 && score<80) ? 'C' :
	//	    (score >= 60&& score<70) ? 'D' : 'F';
	//	     
	////학점출력

	//printf(" \n 당신의 학점은 [%c]입니다.\n\n", grade);



	//char ch;
	//printf("input : ");
	//scanf_s("%c", &ch, 1);

	////입력 문자가 소문자면 .. 대문자로 변환
	//ch = (ch>='a'&& ch <= 'z') ? ch - 32 : ch;

	//printf("\noutput : %c\n", ch);














}

